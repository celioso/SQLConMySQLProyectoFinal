SELECT A.FECHA, SUM(B.CANTIDAD*B.PRECIO) AS FACTURACION
FROM facturas A
INNER JOIN
items B
ON A.NUMERO = B.NUMERO
WHERE A.FECHA = "2024-09-01"
GROUP BY A.FECHA;

CALL sp_venta("2024-09-01", 3, 100);

SELECT COUNT(*) FROM productos;

CALL sp_venta("2024-09-01", 20, 100);